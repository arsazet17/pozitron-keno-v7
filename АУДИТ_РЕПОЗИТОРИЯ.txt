АУДИТ ПОЗИТРОН v7.1

В прежнем ZIP действительно оставались старые файлы и связи:
- max-retro.js был скопирован без изменения;
- источник обновления в первой сборке оставался старым Netlify и давал HTTP 404;
- в промежуточном FIX источник был привязан к репозиторию v7.2;
- архив на 30 107 тиражей пересчитывался при каждом общем render();
- Chrome мог продолжать брать старые JS-файлы без версии в URL.

В REPO FIX:
- собственный источник ./keno-history-v71.json;
- автоматический сброс старых адресов из localStorage;
- загрузка базы без сетевого обновления при старте;
- ленивое построение архива;
- предрасчитанная модель Чётной волны;
- version query ?v=712 для сброса кэша JS/manifest/icons;
- GitHub Actions для обновления базы v7.1;
- проверка синтаксиса JS, JSON, YAML и доступности всех файлов по HTTP.
